server:
  port: 8090
spring:
  application:
    name: eureka
eureka:
  client:
    register-with-eureka: true
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:7900/eureka/
